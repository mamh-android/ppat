<%= form_for(@record_list) do |f| %>
  <% if @record_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@record_list.errors.count, "error") %> prohibited this record_list from being saved:</h2>

      <ul>
      <% @record_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :power_record_id %><br />
    <%= f.number_field :power_record_id %>
  </div>
  <div class="field">
    <%= f.label :cart_id %><br />
    <%= f.number_field :cart_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
