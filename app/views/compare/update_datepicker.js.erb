$('.datepicker').datepicker({
            todayHighlight: true,
            format: "yyyy-mm-dd",
            datesDisabled: [
            <% @datesEnable.each do |image| %>
            		'<%= image.image_date %>',
            <% end %>
            ]
 });