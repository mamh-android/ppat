<?xml version="1.0"?>

<project name="coda_dec" basedir="." default="result">
    <include file="${user.dir}/config/command.xml"/>

    <target name="prepare">
        <PowerCtrlUSB flag="off"/>
    </target>

    <target name="run"  depends="prepare">
        <echo message="run 720p dec..."/>
        <SerialPortCmd cmd="fkcmd" />
        <SystemProperty key="description" value="720p decode, ${VL}"/>
        <PropertyMap key="case" value="720p decode, ${VL}"/>
        <sleep seconds="10"/>
        <PowerCtrlUSB flag="off"/>
        <sleep seconds="10"/>

        <SerialPortCmd cmd="fkcmd" />
        <StatDutyCycle state="1" sleep="20"/>
        <StatDutyCycle state="0" sleep="90"/>
        <SerialPortCmd cmd="${cmd}"/>
        <echo message="Power monitor start to work ..."/>
        <sleep seconds="20"/>
        <MeasurePower server="${power_server}" sample_t="60" voltage="4" save_f="${case_name}_cpu_${cpu}_ddr_${ddr}_vpu0_${vpu0}_${power_port}" port="${power_port}"/>
        <echo message="start collecting duty cycle info..."/>
        <PowerCtrlUSB flag="on"/>
        <sleep seconds="15"/>
        <echo message="start collecting data..."/>
        <PowerResult file_n="${case_name}_cpu_${cpu}_ddr_${ddr}_vpu0_${vpu0}_${power_port}" server="${power_server}"/>
        <copy file="${PT4_SERVER}/${case_name}_cpu_${cpu}_ddr_${ddr}_vpu0_${vpu0}_${power_port}.pt4" todir="${subcase_root}" failonerror="false"/>

    </target>

    <target name="result" depends="run">
        <sleep seconds="20"/>
    </target>

</project>
