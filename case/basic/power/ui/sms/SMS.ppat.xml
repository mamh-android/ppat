<?xml version="1.0"?>

<project name="power_sms_compose" basedir="." default="cleanup">
  	<include file="${user.dir}/config/command.xml"/>
    <target name="reboot" if="${reboot}" >
        <echo message="reboot the device"/>
        <Reboot/>
    </target>
    <target name="run"  depends="reboot">
        <sleep seconds="60"/>
        <ReadThermal flag="thermal_start"/>
        <PowerCtrlUSB flag="off"/>
        <echo message="start  mms activity..."/>
        <SerialPortCmd cmd="am start -n com.android.mms/.ui.ComposeMessageActivity"/>
        <sleep seconds="10"/>
        <echo message="start press WWWWWW..."/>
        <SerialPortCmd cmd="press_w.sh &amp;" />
        <sleep seconds="10"/>
        <echo message="start composing sms ..."/>
        <echo message="mount ..."/>
        <StatDutyCycle state="1"/>
    </target>

    <target name="power" depends="run">
        <MeasurePower server="${power_server}" sample_t="30" voltage="4" save_f="SMS_${power_port}" port="${power_port}"/>
    </target>

    <target name="result" depends="power">
        <echo message="start collecting duty cycle info..."/>
        <StatDutyCycle state="0"/>
        <sleep seconds="2"/>
        <PowerCtrlUSB flag="on"/>
        <ReadThermal flag="thermal_end"/>

        <PowerResult file_n="SMS_${power_port}" server="${power_server}"/>
        <copy file="${PT4_SERVER}/SMS_${power_port}.pt4" todir="${subcase_root}" failonerror="false"/>
        <sleep seconds="50"/>
    </target>

    <target name="cleanup" depends="result">
        <SerialPortCmd cmd="am force-stop com.android.mms" />
        <sleep seconds="10"/>
    </target>

</project>
