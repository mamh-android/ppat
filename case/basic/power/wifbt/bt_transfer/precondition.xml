<?xml version="1.0"?>

<project name="bt_on" basedir="." default="run">
  	<include file="${user.dir}/config/command.xml"/>

    <target name="run">
        <PowerCtrlUSB flag="on"/>
        <sleep seconds="5"/>
        <AdbCmd cmd="adb push resource/test.mp3 /sdcard/"/>
    </target>

</project>
