<?xml version="1.0"?>

<project name="power_game_TempleRun2" basedir="." default="run">
  	<include file="${user.dir}/config/command.xml"/>
    <taskdef resource="net/sf/antcontrib/antcontrib.properties"/>
    <taskdef resource="net/sf/antcontrib/antlib.xml"/>
    <target name="run">
        <PowerCtrlUSB flag="on"/>
        <SerialPortCmd cmd="su"/>
        <AdbCmd cmd="adb install -r resource/apk/Temple_Run2.apk"/>
        <AdbCmd cmd="adb push resource/event/${resolution}/play_templerun.evt /${storage}/"/>

        <UpdateEvent name="${input_device_name}"/>
        <AdbCmd cmd="adb shell busybox sed -i 's/event[0-9]/event${InputEvent}/g' /${storage}/*.evt"/>
    </target>

</project>
