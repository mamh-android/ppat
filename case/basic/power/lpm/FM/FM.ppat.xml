<?xml version="1.0"?>

<project name="power_fm_on" basedir="." default="cleanup">
  	<include file="${user.dir}/config/command.xml"/>
    <target name="reboot" if="${reboot}" >
        <SerialPortCmd cmd="su"/>
        <echo message="reboot the device"/>
        <Reboot/>
    </target>

    <target name="prepare" depends="reboot">
        <echo message="enable airplane mode..."/>

        <SerialPortCmd cmd="am start -a android.intent.action.MAIN -c android.intent.category.LAUNCHER -n com.marvell.ssv.pat/.PATEntryActivity -e method SET_AIRPLANE_MODE --ez MODE false" />
        <sleep seconds="5"/>
        <echo message="turn on wifi ..."/>
        <SerialPortCmd cmd="am instrument -w -e class com.marvell.test.wifi.WifiTest#turnOnWifi com.marvell.test.wifi/.WifiTestRunner" />
        <sleep seconds="5"/>
        <SerialPortCmd cmd="su"/>
    </target>

    <target name="run"  depends="prepare">
        <echo message="turn on fm ..."/>
        <SerialPortCmd cmd="am start -a android.intent.action.MAIN  -n com.marvell.fmradio/.MainActivity" />
        <sleep seconds="30"/>

        <echo message="turn off screen..."/>
        <SerialPortCmd cmd="input keyevent 26" />
        <PowerCtrlUSB flag="off"/>
        <sleep seconds="10"/>
    </target>


    <target name="power" depends="run">
        <echo message="power monitor start work..."/>
        <MeasurePower server="${power_server}" sample_t="50" voltage="4" save_f="FM_${power_port}" port="${power_port}"/>
    </target>
    <target name="result" depends="power">
        <sleep seconds="15"/>
        <PowerCtrlUSB flag="on"/>
        <echo message="start collecting duty cycle info..."/>

        <echo message="start collecting data..."/>
        <sleep seconds="5"/>
        <PowerResult file_n="FM_${power_port}" server="${power_server}" stat_d="false"/>
        <copy file="${PT4_SERVER}/FM_${power_port}.pt4" todir="${subcase_root}" failonerror="false"/>
    </target>

    <target name="cleanup" depends="result">
        <ScreenOn/>
        <ScreenUnlock/>
        <SerialPortCmd cmd="am start -a android.intent.action.MAIN -c android.intent.category.LAUNCHER -n com.marvell.ssv.pat/.PATEntryActivity -e method SET_AIRPLANE_MODE --ez MODE true" />
        <SerialPortCmd cmd="busybox killall com.marvell.fmradio"/>
    </target>

</project>
