<?xml version="1.0"?>

<project name="case_telephony" basedir="." default="run">
    <include file="${user.dir}/config/command.xml"/>
    <taskdef resource="net/sf/antcontrib/antcontrib.properties"/>
    <taskdef resource="net/sf/antcontrib/antlib.xml"/>
    <target name="run">
        <AdbCmd cmd="adb push resource/event/${resolution}/press_yecx.evt /${storage}/"/>
        <AdbCmd cmd="adb push resource/event/${resolution}/create_msg_to_10086.evt /${storage}/"/>
        <AdbCmd cmd="adb push resource/event/${resolution}/delete_msg.evt /${storage}/"/>
        <UpdateEvent name="${input_device_name}"/>
        <AdbCmd cmd="adb shell busybox sed -i 's/event[0-9]/event${InputEvent}/g' /${storage}/*.evt"/>
    </target>
</project>
